import React from "react";
import { View, Text, StyleSheet } from "react-native";

const ImageDetails = () => {
  return (
    <View>
      <Text>This is Image Details</Text>
    </View>
  );
};

const style = StyleSheet.create({});

export default ImageDetails;
